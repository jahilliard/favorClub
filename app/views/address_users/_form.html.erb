<%= form_for(@address_user) do |f| %>
  <% if @address_user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@address_user.errors.count, "error") %> prohibited this address_user from being saved:</h2>

      <ul>
      <% @address_user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :address_id %><br>
    <%= f.number_field :address_id %>
  </div>
  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.number_field :user_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
